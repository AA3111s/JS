# Method of primitive data types

In JavaScript, primitive data types (such as strings, numbers, booleans, null, undefined, and symbols) do not have methods or properties. However, when you try to access a method or property on a primitive value, JavaScript temporarily wraps the primitive in an object wrapper (like String, Number, or Boolean) to allow you to use methods.
